# 🎮 롤 내전 팀 배정 웹사이트 기획서

> **프로젝트명**: LoL Draft 
> **작성일**: 2026-07-29
> **기술 스택**: HTML / CSS / JavaScript + Firebase Realtime Database

---

## 1. 프로젝트 개요

리그 오브 레전드 내전 시 10명을 두 팀으로 나누는 과정을 자동화하는 웹 서비스.
방장이 방을 만들어 링크를 공유하면, 참가자들이 자신의 정보를 입력하고 실시간으로 팀이 배정된다.

### 핵심 목표
- 라인 충돌 최소화 + 팀 간 점수 차 최소화를 동시에 달성
- 방장의 수동 개입(티어 조정, 인원 교체)을 UI로 쉽게 지원
- 실시간 동기화로 10명이 다 찼는지 모두가 즉시 확인 가능

---

## 2. 기술 스택

| 항목 | 선택 | 이유 |
|------|------|------|
| 프론트엔드 | HTML + CSS + Vanilla JS | 별도 빌드 툴 없이 바로 배포 가능 |
| 실시간 DB | Firebase Realtime Database | 10명 인원 현황 실시간 반영, 무료 플랜으로 충분 |
| 호스팅 | Firebase Hosting 또는 GitHub Pages | 정적 파일 무료 배포 |

---

## 3. 화면 흐름 (User Flow)

```
[방장]
  └─ 방 생성 → 고유 링크 발급 → 링크 공유

[참가자]
  └─ 링크 접속 → 정보 입력 → 대기실 진입

[대기실] (실시간 인원 표시)
  ├─ 현재 명단 (최대 10명)
  └─ 대기 명단 (초과 인원)

[방장 전용]
  ├─ 티어 조정 (△ ▽)
  ├─ 참가자 삭제 / 대기명단 이동 (드래그 앤 드롭)
  └─ [팀 배정 버튼] 클릭

[팀 배정 결과]
  ├─ 블루팀 / 레드팀 표시
  ├─ 밸런스 지표 (황밸 / 그럭저럭 / 언밸)
  └─ [다음 조합 보기] 버튼
```

---

## 4. 참가자 입력 정보

| 필드 | 타입 | 설명 |
|------|------|------|
| 닉네임 | 텍스트 | 라이엇 닉네임 (닉네임#태그 형식) |
| 희망 라인 | 단일 선택 | 탑 / 정글 / 미드 / 원딜 / 서폿 중 1개 |
| 부라인 | 다중 선택 | 0개 ~ N개 선택 가능 (없음도 가능) |
| 티어 | 드롭다운 | 실버3이하 ~ 마스터+ |
| LP (마스터 이상) | 숫자 입력 | 마스터/그랜드마스터/챌린저 선택 시 추가 입력 |

> **티어 입력 2단계 UI**: 티어 선택 → 마스터 이상이면 LP 입력창 추가 표시

---

## 5. 대기실 기능 상세

### 5-1. 현재 명단 (Active List)
- 최대 10명, 선착순 입장
- 각 플레이어 카드: 닉네임 + 희망라인 아이콘 + 티어 아이콘 표시
- 클릭 시 → op.gg 새 탭으로 연결 (`https://op.gg/summoners/kr/{닉네임}`)

### 5-2. 대기 명단 (Waiting List)
- 10명 이후 참가 신청자들이 대기
- 대기자가 10명 이상이 되면 → **20명 4팀 체제 전환 버튼** 활성화 (방장 전용)

### 5-3. 방장 전용 관리 기능
- **티어 조정**: 각 참가자 카드에 △(올리기) / ▽(내리기) 버튼으로 한 단계씩 조정
- **드래그 앤 드롭**: 현재 명단 ↔ 대기 명단 간 자유롭게 이동
- **참가자 삭제**: 카드의 X 버튼으로 강제 퇴장
- **방 만료 설정**: 방 생성 시 만료 시간 설정 (기본 24시간), 만료 후 Firebase에서 자동 삭제

---

## 6. 팀 배정 알고리즘

### 6-0. 사전 검증 (팀배정 버튼 클릭 시)
팀 배정이 구조적으로 불가능한 경우 알림 표시 후 중단:
- 활성 참가자 수 검증 (정확히 10명 필요)
- 특정 라인에 배정 가능한 인원(희망+부라인+상관없음 합산)이 2명 미만인 경우
- 라인 배정 가능한 조합이 하나도 존재하지 않는 경우

### 6-1. STEP 1: 점수 산정

`scores.js` (score.md 기반) 점수 테이블을 참조하여 각 플레이어의 (실효)티어 및 마스터+ LP 기반 라인별 점수를 계산.

```
플레이어 점수 예시:
  A (다이아1, 희망:미드, 부라인:탑)
    → 미드: 38.7점 / 탑: 35.7점

  B (에메랄드2, 희망:미드, 부라인:없음)
    → 미드: 33.0점
```

### 6-2. STEP 2: 라인 배정 (백트래킹 완전탐색)

**처리 순서:**

1. **희망 라인 및 부라인 탐색**
   - 참가자의 입장 순서(선착순)를 기준으로 라인 배정 시도
   - 각 플레이어별로 `주라인 우선 시도 → 부라인 시도` 순서로 배치 ('상관없음' 선택 시 5개 라인 모두 후보)

2. **실현 가능성 검증 및 가지치기 (Feasibility Check)**
   - 재귀 탐색 진행 중, 남은 인원수로 남은 라인 슬롯을 채울 수 있는지 실시간 확인
   - 불가능한 배정 경로는 즉시 탐색 중단(Pruning)하여 유효한 라인 배정 조합 수집 (최대 30개)

### 6-3. STEP 3: 팀 나누기 & 밸런스 최적화

**처리 순서:**

1. **진영 조합 계산 (최대 30개 라인 배정 × 32가지 = 최대 960가지)**
   - 백트래킹으로 탐색된 라인 배정 조합마다 각 라인의 블루/레드팀 선택 ($2^5 = 32$가지) 계산
   - 블루팀 총점과 레드팀 총점 계산

2. **최적 조합 선별 (상위 10개)**
   - `|블루팀 총점 - 레드팀 총점|` (점수 차이) 기준 오름차순 정렬
   - 블루/레드 진영 스왑 중복을 제거한 후 상위 10개 조합 선별하여 저장 ("다음 조합 보기" 제공)

### 6-4. 밸런스 지표 표시

팀 간 총점 차이에 따라 결과 화면에 밸런스 라벨 표시:

| 점수 차 | 표시 | 클래스 | 비고 |
|--------|------|------|------|
| 0 ~ 5점 | 🟡 황밸 | `balance-gold` | 최우수 밸런스 |
| 5 ~ 15점 | 🟢 밸런스 양호 | `balance-green` | 양호한 밸런스 |
| 15점 초과 | 🔴 언밸 | `balance-red` | 참가자/라인 조정 권장 |

---

## 7. 팀 배정 결과 화면 레이아웃

```
┌──────────────────────────────────────────┐
│  황밸  |  블루팀 128.4 vs 레드팀 126.1    │
├──────────────────┬───────────────────────┤
│     블루팀        │       레드팀           │
├──────────────────┼───────────────────────┤
│ [탑]  닉네임A     │ [탑]  닉네임F          │
│ [정글] 닉네임B   │ [정글] 닉네임G         │
│ [미드] 닉네임C   │ [미드] 닉네임H         │
│ [원딜] 닉네임D   │ [원딜] 닉네임I         │
│ [서폿] 닉네임E   │ [서폿] 닉네임J (부)    │
├──────────────────┴───────────────────────┤
│  [이전 조합]              [다음 조합]      │
│        [카카오톡 공유용 텍스트 복사]        │
└──────────────────────────────────────────┘
```

- 각 플레이어 카드 클릭 → op.gg 연동
- 라인별 아이콘 + 티어 이미지 표시
- 배정된 라인이 부라인인 경우 `(부)` 뱃지 표시

---

## 8. UI/UX 가이드라인

- **다크 테마** 기반 (롤 클라이언트 무드)
- 라인 아이콘: 라이엇 공식 라인 아이콘 사용
- 티어 아이콘: 라이엇 공식 티어 배지 이미지 사용
- 모바일 반응형: 참가자들이 폰으로 접속하는 경우 다수 → 모바일 우선 설계
- 드래그 앤 드롭: PC에서는 마우스 드래그, 모바일에서는 터치 드래그 지원

---

## 9. Firebase 데이터 구조

```json
/rooms/{roomId}/
  ├── meta/
  │   ├── createdAt: timestamp
  │   ├── expiresAt: timestamp
  │   ├── hostUid: string
  │   └── mode: "5v5" | "4team"
  ├── players/
  │   └── {playerId}/
  │       ├── nickname: string
  │       ├── mainLine: "top" | "jungle" | "mid" | "adc" | "support"
  │       ├── subLines: ["top", "mid", ...]
  │       ├── tier: string        (예: "다이아1", "에메랄드2")
  │       ├── lp: number          (마스터 이상만)
  │       ├── adjustedTier: string  (방장이 조정한 티어, 없으면 tier 사용)
  │       ├── status: "active" | "waiting"
  │       └── joinedAt: timestamp
  └── result/
      ├── blue: [playerId, ...]
      ├── red: [playerId, ...]
      ├── lineAssignment: { playerId: "top" | "jungle" | ... }
      ├── isSubLine: { playerId: boolean }
      └── scoreDiff: number
```

---

## 10. 개발 우선순위 (Phase)

### Phase 1: 핵심 기능 (MVP)
- [ ] Firebase 연동 + 방 생성/참가 (고유 링크 공유)
- [ ] 참가자 정보 입력 폼 (티어 2단계 UI 포함)
- [ ] 실시간 인원 현황 대기실 화면
- [ ] 팀 배정 알고리즘 구현 (점수산정 → 라인배정 → 팀분할)
- [ ] 팀 배정 결과 표시

### Phase 2: 관리 기능
- [ ] 방장 전용: 티어 조정 (△ ▽)
- [ ] 방장 전용: 드래그 앤 드롭 인원 관리
- [ ] 대기 명단 기능
- [ ] "다음 조합 보기" (차순위 탐색)
- [ ] 방 만료 시간 + 자동 삭제

### Phase 3: 편의 기능
- [ ] op.gg 클릭 연동
- [ ] 라인/티어 이미지 UI 적용
- [ ] 밸런스 지표 (황밸/그럭저럭/언밸)
- [ ] 카카오톡 공유용 텍스트 복사
- [ ] 모바일 반응형 최적화

### Phase 4: 추가 기능 (선택)
- [ ] 텍스트 파싱 일괄 입력 (예시 포맷 복사 버튼 제공)
- [ ] 20명 4팀 체제 전환
- [ ] (부) 뱃지 외 부라인 배정자 시각적 강조

---

## 11. 확정 사항

| # | 항목 | 결정 내용 |
|---|------|----------|
| B | 희망 라인 겹칠 때 우선순위 | **선착순** |
| E | 부라인 없는 사람이 희망 라인 겹칠 때 | **6-0 사전 검증에서 팀배정 불가 알림으로 처리** |
